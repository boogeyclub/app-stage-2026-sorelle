# Copy this file to .env before starting PostgreSQL or the Spring Boot service.
# These values are for local development only; use secure deployment-specific credentials elsewhere.
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=connectmarket
POSTGRES_USER=connectmarket
POSTGRES_PASSWORD=connectmarket_dev_password
