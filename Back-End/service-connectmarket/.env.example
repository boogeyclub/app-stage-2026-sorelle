# Copy this file to .env to override the connection to your locally installed PostgreSQL server.
# PostgreSQL must already be running and contain the configured database/user.
# These values are for local development only; use secure deployment-specific credentials elsewhere.
POSTGRES_HOST=localhost
POSTGRES_PORT=6000
POSTGRES_DB=cacaomarketcm
POSTGRES_USER=sorelle
POSTGRES_PASSWORD=root1234

# SMTP configuration for account-confirmation emails. Copy these values to the local .env file;
# never commit real SMTP credentials.
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_USERNAME=replace-with-smtp-username
MAIL_PASSWORD=replace-with-smtp-password
MAIL_SMTP_AUTH=true
MAIL_STARTTLS=true
REGISTRATION_MAIL_FROM=no-reply@cacaomarket.example

# Public URL placed in the confirmation email. Keep the backend confirmation endpoint and append no token;
# the service adds a secure token query parameter itself.
REGISTRATION_CONFIRMATION_URL=http://localhost:8080/api/auth/registration/confirm

# Comma-separated origins only when the frontend calls the API directly instead of through its /api proxy.
APP_CORS_ALLOWED_ORIGINS=http://localhost:4200
