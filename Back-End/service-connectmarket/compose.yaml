services:
  postgres:
    image: postgres:17-alpine
    container_name: connectmarket-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-connectmarket}
      POSTGRES_USER: ${POSTGRES_USER:-sorelle}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-root1234}
    ports:
      - "127.0.0.1:${POSTGRES_PORT:-6000}:5432"
    volumes:
      - connectmarket-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  connectmarket-postgres-data:
